export interface ThemeConfig {
  name: string;
  background: string;
  foreground: string;
  cardBackground: string;
  borderColor: string;
  fontFamily: string;
  colors: string[];
  gridColor: string;
  axisColor: string;
  titleColor: string;
  textColor: string;
}
