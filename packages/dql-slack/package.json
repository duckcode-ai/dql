{
  "name": "@duckcodeailabs/dql-slack",
  "version": "1.3.6",
  "description": "DQL Slack front-end: slash-command bot that answers via the block-first agent loop",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc -b",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prepublishOnly": "pnpm run build"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/duckcode-ai/repo.git",
    "directory": "packages/dql-slack"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@duckcodeailabs/dql-agent": "workspace:*",
    "@duckcodeailabs/dql-core": "workspace:*",
    "@duckcodeailabs/dql-project": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
