{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "private": true,
  "description": "Acme Bank DQL analytics project.",
  "scripts": {
    "notebook": "dql notebook",
    "compile": "dql compile",
    "lineage": "dql lineage",
    "app:build": "dql app build",
    "agent:reindex": "dql agent reindex",
    "verify": "dql verify .",
    "doctor": "dql doctor",
    "validate": "dql validate",
    "certify:cards": "dql certify blocks/cards/card_approval_rate.dql --connection duckdb"
  },
  "devDependencies": {
    "@duckcodeailabs/dql-cli": "^1.6.1"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
