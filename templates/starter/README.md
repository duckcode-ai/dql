# DQL Starter

This template is the minimal public DQL project layout.

```text
blocks/
semantic-layer/
```

Use the CLI from the root `dql` workspace or install published `@dql/*` packages in your own project.
