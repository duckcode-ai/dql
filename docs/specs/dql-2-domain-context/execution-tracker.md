# Integration execution tracker

The integration owner is the sole editor of status in this file during active
delivery. Do not use this tracker to change requirements; amend the relevant
spec and acceptance matrix first.

| Checkpoint | Branch/worktree | Base SHA | Head SHA | Owner | Status | Verified by | Evidence/notes |
| ---------- | --------------- | -------- | -------- | ----- | ------ | ----------- | -------------- |
| Spec freeze | `codex/dql-2-dbt-first-modeling` | `00570007019c055a4000cd1069262fa9d053a52d` | TBD | integration owner | in progress | TBD | RFC 0003 + this pack; head filled after checkpoint commit |
| W01 core contracts and identity | `codex/dql2-w01-core-contracts` | TBD | — | unassigned | pending | — | — |
| W02 snapshot and scale | `codex/dql2-w02-snapshot` | TBD | — | unassigned | pending | — | — |
| W03 onboarding and discovery | `codex/dql2-w03-onboarding` | TBD | — | unassigned | pending | — | — |
| W04 agent context and governance | `codex/dql2-w04-agent-context` | TBD | — | unassigned | pending | — | — |
| W05 Domain Studio and products | `codex/dql2-w05-domain-studio` | TBD | — | unassigned | pending | — | — |
| W06 migration and security | `codex/dql2-w06-migration-security` | TBD | — | unassigned | pending | — | — |
| W07 integration/evals/performance | `codex/dql2-w07-integration` | TBD | — | unassigned | pending | — | — |
| W08 OSS release/docs | `codex/dql2-w08-release` | TBD | — | unassigned | pending | — | — |

## Integration protocol

1. Freeze specs and record the actual integration base SHA.
2. Create isolated workstream branches/worktrees from the recorded dependency
   commit. Never let concurrent agents edit the same worktree.
3. Provide assignments using `agent-handoff-template.md`, including owned and
   prohibited globs.
4. Implementer runs focused tests, records evidence, and marks requirements
   `implemented` only.
5. A different verifier performs read-only review/tests and reports pass/fail.
6. Integration owner applies the verified commit, resolves conflicts, updates
   this tracker/matrix, and reruns affected package gates.
7. After every UI/runtime integration, restart the built CLI-backed Jaffle or
   dedicated fixture and verify visible behavior.
8. Before final release, run all gates in `09-fixtures-evals-and-release-gates.md`.

## Conflict/blocker log

| Date | Workstream | Requirement | Conflict/blocker | Decision/reference | Status |
| ---- | ---------- | ----------- | ---------------- | ------------------ | ------ |
| 2026-07-29 | W04/W05/W07 | `AGT-013`, `API-008` | Server thread loading replaced the client history before execution, but early routing, meaning resolution, and Research planning still depended on the now-empty client field. Long-thread compaction also stored untyped prose, configured embeddings were not used for older-turn recall, and UI remounts reconstructed every compact turn as completed. | Keep Ask, Notebook AI, and Block AI threads separate. Build one bounded server-owned conversation envelope per request and use it through routing, meaning resolution, planning, and execution; persist terminal status/stop reason; add deterministic source-attributed trust states to compacted turns; use the configured local embedding provider with deterministic fallback; and preserve blocked/review/clarification state during UI hydration. The full agent suite (1,201 tests), focused CLI provider and lifecycle tests, affected production builds, and 41 focused Notebook panel tests pass. The full CLI runtime file cannot bind loopback servers in the restricted implementation sandbox and remains an independent environment check. | implemented |
| 2026-07-29 | W04/W05/W07 | `API-008` | Conversation turns were persisted under rebuildable `.dql/cache`, while the Ask sidebar depended on origin-scoped browser `localStorage`; clearing caches, changing the Notebook port/profile, or rebuilding/upgrading the frontend could therefore delete or hide history. | Store private conversation and bounded run databases under ignored durable `.dql/local`; migrate the legacy conversation SQLite database through the backup API so committed WAL state survives; treat browser storage as a rendering cache; rebuild Ask Recent from server threads; archive the durable thread when the user deletes a chat; and label new Ask, Notebook, and Block threads by surface. | implemented |
| 2026-07-29 | W04/W05/W07 | `API-005`, `UI-011` | Ask parameter Apply falsely reported source drift when the receipt fingerprint included the renderer's final newline but artifact normalization removed it; Ask also exposed only parameters created during generation, so a user could not explicitly add a filter from returned columns. Equivalent parent rerenders and rapid double-click Apply could reset or race a locally derived parameter artifact. | Preserve exact executable source bytes, accept only the bounded final-newline variant for legacy receipts, and retain substantive drift rejection. Transient semantic Ask results now allow explicit typed filters from returned dimensions, reuse the shared DQL parameter controls, preserve locally derived inputs across equivalent rerenders, and execute Apply single-flight. The flow creates a new review-required execution artifact without another AI search, saved-source mutation, certification, metric/HAVING guesses, or raw-SQL rewriting. Focused core/CLI/UI tests and production builds pass; built-CLI browser proof reran `top_n = 9` plus `location_name = Philadelphia`, retained the input through double-click and inspector rerenders, and returned filtered rows without the false drift error. | implemented |
| 2026-07-29 | W05/W08 | `PRD-001`, `UI-001`, `UI-006`, `MIG-001` | The reference layout and proposed folder UI placed new Apps and Notebooks inside Domain Packages, conflicting with the accepted global-product contract and risking path churn for existing dbt/DQL repositories. A later UI pass also exposed internal knowledge/governance inventories as noisy top-level Domain destinations. | Preserve canonical root `apps/` and `notebooks/`, validate and persist `ProductDomainContext`, expose those products as Domain Studio backlinks and virtual Notebook folders, keep legacy domain-local products readable, and use domain-first physical folders only for DQL-owned package source. Keep Domain navigation focused on Models, Skills, Blocks, Notebooks, and Apps; selecting a product backlink opens its exact canonical editor/view while compiled governance evidence remains internal to retrieval, validation, and task-specific inspectors. | implemented |
| 2026-07-23 | W02/W04/W05/W07 | `CTX-005`, `AGT-014`, `API-006`, `API-007`, `UI-012`, `PERF-001`, `E2E-008`, `E2E-014` | Enterprise Snowflake testing exposed eager all-column materialization, missing compiler-target/execution-target identity, lost semantic trace/connector diagnostics, and cross-surface execution drift despite prior implementation evidence. | Specs 07/10 amended; target-bound execution, bounded schema search/streaming, canonical receipts, structured failures, adapter pinning, and built-CLI regression coverage implemented on `codex/target-bound-semantic-runtime`. Independent enterprise-fixture verification remains required. | implemented |
| 2026-07-23 | W03/W04/W05/W07 | `API-004`, `API-007`, `UI-009`, `UI-012`, `SEC-004`, `E2E-008`, `E2E-014` | Real dbt Cloud/local MetricFlow testing still allowed local semantic metadata to be paired with an unverified cloud environment, wrapped catalog drift as a generic setup error, lost physical binder evidence, and let rejected async handlers terminate the Notebook server; Snowflake also ran on unsupported Node 26. | `codex/semantic-runtime-stabilization` persists the complete paginated cloud metric inventory and catalog proof on explicit Test & Apply, fails closed with `SEMANTIC_SOURCE_DRIFT`, target-binds the cloud proof, preflights compiled SQL with identifier/excerpt evidence, preserves failures in Trust & Steps, catches every request rejection, and rejects unsupported Snowflake Node majors before startup. Focused runtime/agent/UI tests and production builds pass; designated enterprise fixture and built-CLI browser verification remain independent-verifier work. | implemented |
| 2026-07-23 | W01/W04/W06/W07 | `AGT-014`, `API-006`, `API-007`, `SEC-004`, `E2E-014` | Snowflake exposes one account as an immutable locator and as the preferred `organization-account_name` identifier. DQL compared those forms as unrelated strings (and dbt Cloud also required an obsolete exact target fingerprint), while failed `CURRENT_*` acquisition silently saved configured fallback data. This produced false account drift across dbt Cloud and local MetricFlow. | Warehouse identity now records canonical name, locator, account name, and organization; account comparison uses bounded aliases while database/schema/role/warehouse remain strict; legacy bindings migrate through field-aware comparison; and failed Snowflake identity observation blocks apply/execution. Core and CLI regression suites cover legacy locator compatibility, canonical identifiers, true account drift, and fail-closed acquisition. Independent enterprise Snowflake verification remains required. | implemented |
| 2026-07-23 | W01/W03/W04/W05/W07 | `ID-001`, `CONTRACT-002`, `AGT-014`, `API-004`, `UI-009`, `E2E-008` | Enterprise dbt projects expose measures both as technical measure projections and business metrics, derived metrics can lose semantic-model ownership, and the Notebook composer collapses model-qualified dimension variants back to bare names. The result is duplicate/uncategorized catalog entries, valid dimensions hidden for some metrics, and unrelated same-named dimensions enabled for others despite the metric-first compatibility contract. | `codex/semantic-model-composer` now records direct and derived semantic-model ownership, excludes projected measures from business-metric discovery, preserves exact model-scoped dimension identities, groups the catalog by semantic model, exposes only runtime-compatible dimensions after metric selection, and feeds the same compatibility evidence to agent search and member selection. Focused core/agent/CLI/UI tests and production builds pass; independent enterprise-fixture verification remains required. | implemented |
| 2026-07-23 | W04/W05/W07 | `API-006`, `API-007`, `UI-009`, `UI-012`, `E2E-008`, `E2E-014` | Notebook cells trusted duplicate IDs, discarded semantic compiler/target receipts, stopped resolving completed upstream results after the transient success state returned to idle, and allowed an older aborted run or failed dependency branch to overwrite or contaminate another cell run. | `codex/semantic-model-composer` now repairs duplicate IDs on load/store, assigns and validates a unique run ID per cell request, prevents stale completions from mutating newer state, isolates failed dependency branches during Run all, pins composer preview and inserted DQL cells to one execution target, persists compiler/SQL/target/receipt evidence, and exposes it in each cell's Trust & Steps inspector. Full CLI/core/agent/notebook suites and production builds pass; the built `dql notebook` fixture visibly ran the SQL route and showed `Connection: default`, the run ID, route, SQL, duration, and result in Trust & Steps. Independent enterprise verification remains required. | implemented |
| 2026-07-23 | W04/W05/W07 | `API-006`, `UI-001`, `UI-009`, `E2E-008` | Notebook semantic insertion remained disabled until Preview completed, Block Studio exposed unrelated dimensions when compatibility failed, its sidebar bypassed the visual composer, raw-SQL visual edits normalized away parameter/binding sections, and saved blocks had no guarded delete path. | `codex/block-studio-semantic-wiring` makes Preview optional for insertion after a successful compatibility plan, reuses the metric-first composer in Notebook and Block Studio, shows only fresh common governed dimensions, reconciles stale selections fail-closed, preserves raw-SQL parameters/policies/bindings through targeted source edits, adds visual-source parity, and deletes one exact block plus its companion through a confirmed API/UI flow. Full UI/CLI suites and production builds pass; built-CLI browser proof confirms Notebook insertion, Block Studio application, compatibility-filtered dimensions, raw-SQL visual parity, and guarded delete. Independent enterprise-fixture verification remains required. | implemented |
| 2026-07-25 | W04/W05/W07 | `AGT-015`, `AGT-017`, `AGT-018`, `AGT-020`, `UI-012`, `UI-013`, `E2E-012`, `E2E-014` | Ask collapsed explicitly requested measures to one, single-Y visualization defaults hid additional returned measures, deterministic narration described only the ranking measure, SQL ambiguity checks skipped CTE queries and guessed the first relation during repair, and DQL/SQL inspector previews lacked a direct editable-notebook handoff. | Implemented on `main` from `b4b7b087`: explicit metric selection and immutable plan adaptation preserve all selected measures; multi-measure results use a lossless table contract; deterministic narration reports the leading and supporting measures; SQL references are validated per SELECT/CTE scope and ambiguous ownership fails closed; repair prompts preserve requested outputs; and inspector DQL/SQL actions insert, focus, and scroll to editable notebook cells. Focused core, agent, CLI, and notebook tests pass. Full built-CLI fixture and enterprise Snowflake verification remain independent-verifier work. | implemented |
| 2026-07-25 | W04/W05/W07 | `API-006`, `UI-001`, `UI-009`, `E2E-008` | Block Studio exposed delete only through the active-block flow, opened pasted raw SQL without the complete visual authoring controls, allowed visual/source state to lag until save, and flattened Git-backed blocks by domain without nested-folder organization or safe move semantics. | Block Studio now uses one live draft contract across semantic, raw-SQL, and DQL-source authoring; raw SQL opens the complete visual builder; metadata and source changes round-trip before save; nested domain folders are path-confined and saving a new folder moves the exact block and semantic companion; and library, detail, and toolbar surfaces expose guarded deletion. The notebook suite, focused CLI artifact suite, and production builds pass. Built `dql notebook` browser QA against an isolated `dbt-first-commerce` fixture visibly confirmed unsaved SQL-to-DQL synchronization, nested-folder creation/rendering, and row-level deletion. Independent enterprise verification remains required. | implemented |
| 2026-07-26 | W01/W04/W05/W07/W08 | `CONTRACT-002`, `AGT-017`, `AGT-021`, `API-006`, `UI-009`, `UI-016`, `E2E-015` | Ask and Block AI split exact quoted metrics between metric and measure catalogs, capped explicit selections, treated technical identifiers as filter members, lost prior artifact metrics in “other metrics” follow-ups, rejected the sentinel `uncategorized` domain, and emitted different singular/array semantic DQL shapes across AI, Notebook, Block Studio, formatting, and CLI scaffolding. | One core semantic-block renderer now owns the canonical owner-optional DQL contract; formatter migration, manual and Notebook authoring, Ask/Block AI artifacts, runtime imports, MCP reuse, and `dql new semantic-block` consistently emit `metrics = [...]` and `dimensions = [...]` while compiled SQL remains execution evidence. Exact five-metric planner/composer and follow-up regressions pass; core has 534 passing tests and agent has 1,123. CLI/Notebook production builds pass. Built `dql notebook` browser QA visibly confirmed canonical empty arrays and that an `uncategorized` Block AI request reaches the configured provider instead of failing domain resolution; the isolated fixture lacked a reachable Claude login, so provider-backed multi-metric and ownerless commit verification remain independent-verifier work. | implemented |
| 2026-07-26 | W04/W05/W07 | `AGT-017`, `API-007`, `UI-012`, `UI-013`, `E2E-015` | A semantic SQL compilation failure could collapse into a message-only blocked run, leaving no attempted DQL/SQL repair inputs; conversation persistence also discarded compiled SQL and trust fields, so inspector sections could disappear after navigation. | Semantic compilation now materializes the canonical DQL attempt before invoking the adapter, retains any attempted SQL, classifies the stable analytical failure, and returns the original immutable plan plus safe Notebook repair actions. Conversation turns preserve the full bounded DQL artifact contract; blocked analytical inspectors always retain Plan, DQL, SQL, Trust & Steps, and explicit unavailable states; and the standalone Ask surface opens retained DQL or SQL as an editable Notebook cell with trust metadata intact. Agent has 1,124 passing tests, Notebook has 124, focused CLI failure persistence passes, and all three production builds pass. The built-CLI fixture launched successfully, but its configured Claude subscription was unreachable, so provider-backed failure-path browser verification remains independent-verifier work. | implemented |
| 2026-07-26 | W04/W05/W07 | `CONTRACT-002`, `AGT-021`, `API-008`, `UI-014`, `UI-015`, `UI-016`, `E2E-015` | The first parity pass still left new Block Studio chats in generic `auto` mode, cold Block AI on the legacy write-on-generation builder, answer-to-block reuse writing through the importer before consent, and the legacy proposal writer emitting singular `metric =` plus embedded SQL for semantic drafts. | Block Studio now always requests the explicit block route; both cold and carried-artifact Block AI runs reuse the governed answer executor and return one transient ownerless review artifact; only Add to Block Studio writes and opens the saved draft; and semantic proposal generation uses the core canonical `metrics = [...]` / `dimensions = [...]` renderer with compiled SQL retained separately. The focused HTTP integration proves generation creates no block/domain files; the agent suite has 1,128 passing tests, core has 534, CLI has 571 plus 3 skipped, Notebook has 125, and all affected production builds pass. Built `dql notebook` visibly proves `User selected block mode` and `dql block draft`; its configured Claude subscription remains unreachable, so provider-backed Add-to-Block-Studio browser verification remains independent-verifier work. | implemented |
