metrics:
  - name: total_revenue
    label: Total Revenue
    description: Sum of recognized revenue
    domain: revenue
    sql: SUM(amount)
    type: sum
    table: fct_revenue

dimensions:
  - name: segment_tier
    label: Segment Tier
    description: Customer segment tier
    sql: segment_tier
    type: string
    table: fct_revenue
